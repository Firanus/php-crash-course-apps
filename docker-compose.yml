version: '2'
services:
  server:
    image: php:7.1-apache
    ports:
      - "80:80"
    volumes:
      - ./:/var/www/html
    links:
      - mysql

  mysql:
    image: mysql:latest
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=root